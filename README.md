# Project Tutwiler

Secure messaging platform for university dorms.

## Quick Start

1. **Install dependencies** (required before starting the server):
   ```bash
   cd api
   npm install
   ```

2. **Start the server**:
   ```bash
   npm start
   ```
   Or for development with auto-reload:
   ```bash
   npm run dev
   ```

3. Open `http://localhost:5000` in your browser.

## Project Structure

- `api/` - Node.js/Express backend API
- `database/` - SQL schema and migration files
- `frontend/` - Frontend JavaScript modules
- `scripts/` - Frontend scripts
- `styles/` - CSS stylesheets

## Setup Details

See `api/README.md` for detailed API setup instructions and endpoint documentation.

See `database/README.md` for database setup instructions.

## Prerequisites

- Node.js 16+ installed
- MySQL database (see `database/README.md` for setup)

